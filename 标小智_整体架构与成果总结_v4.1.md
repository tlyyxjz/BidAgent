# 标小智 — 整体架构与成果总结（v4.1 对齐版）

> **用途**：供千问 3.8 Max 审查项目架构合理性、功能完整度与合规性
> **生成时间**：2026-08-04
> **当前状态**：GOAI 2026 初赛提交前 · feature/glm-w4-k3-data 分支 · commit 396a3e8 · 1882 测试通过（成稿时点；最新 1979 passed / 90.30%）


> **⚠ 口径说明（2026-08-07 更新）**：本文档成稿于 v4.1 收官期（2026-07），文中部分数字为该时点快照。最新口径：金标 **598 篇**（非 99/107 篇）、D 组 field_precision **96.44%**、unjustified_rate **0.00%**、**1979** 测试 · 覆盖率 **90.30%**。以正式版提交材料与实测数据口径声明为准。

---

## 一、项目定位

| 维度 | 内容 |
|---|---|
| 一句话定位 | 面向供应链金融贷前尽调与企业采购核验的可验证招投标数据引擎 |
| 核心用户 | 供应链金融贷前尽调人员 |
| 系统定位 | 位于金融风控的**数据准备与事实核验**环节，为后续人工尽调或其他风控系统提供带证据的招投标数据，而非直接替代风控决策 |
| 核心差异化 | LLM 只生成字段和证据候选，**确定性程序负责在原文快照中搜索验证**，找不到依据的字段一律标记为无依据不输出 |
| 赛事 | GOAI 世界人工智能开源大赛 · 无界应用赛道 · AI+金融方向 |

### 8 条核心差异化（v4.1 §1.4）

1. 每个字段可绑定多段原文证据，区分主证据、限定条件和推导输入
2. LLM 只生成字段和证据候选，确定性程序负责验证
3. 区分项目、公告、来源页面和页面版本四层实体
4. 区分官方原始发布、官方转载、商业转载和索引页面
5. 识别同源转载，避免将转载数量误判为独立交叉验证
6. 将抽取支持度、来源质量和交叉验证状态独立保存
7. 使用独立金标测试集验证准确率、覆盖率和无依据输出率
8. 只输出可解释的公开招投标活动观察信号，**不输出信用评分**

---

## 二、整体架构

### 2.1 四层实体数据模型（v4.1 §4）

```
TenderProject（采购项目）
  └── TenderNotice（业务公告）
        ├── NoticeParticipant（公告参与关系）
        └── NoticeSource（来源页面）
              └── NoticeVersion（抓取版本）
                    └── ExtractedField（抽取字段）
                          └── FieldEvidenceLink
                                └── Evidence（字段证据）
```

**辅助实体**：Organization（组织机构）、NoticeParticipant（参与关系）、ProjectIdentifier（项目标识）、FactAssertionKey（事实断言键）。

**设计原则**：所有核心实体使用无业务含义的内部稳定主键（ULID），业务编号、URL 和标题哈希仅作外部标识与匹配证据，不得直接作为永恒不变的唯一主键。

### 2.2 六 Agent 协同架构

```
意图解析 Agent → 采集执行 Agent → 数据加工 Agent → 质量保障 Agent → 金融分析 Agent → 报告交付 Agent
```

| Agent | 职责 | 核心技术 |
|---|---|---|
| 意图解析 | 自然语言拆解为关键词/地区/预算/时间窗口 | LLM 意图理解 |
| 采集执行 | 调度官方平台采集器，合规抓取 | 域名级8秒频率限制 + robots.txt + 来源白名单 |
| 数据加工 | 去重、字段对齐、同源转载识别 | SimHash 汉明距离 ≤ 3 + 事实断言键 |
| 质量保障 | LLM 生成候选 + 确定性程序验证 | 5级降级匹配 + 双坐标证据映射 |
| 金融分析 | 六维公开活动观察信号 | 严格不输出信用评分 |
| 报告交付 | Word 报告 + cron 定时推送 + 幂等去重 | SMTP + Webhook HMAC + content_hash |

### 2.3 可验证抽取引擎（核心差异化）

**流程**：LLM 生成字段和证据候选 → 确定性程序在原文快照中搜索验证 → 找不到依据标记为无依据不输出

**5 级降级匹配**：
1. L1 精确匹配
2. L2 空白归一化
3. L3 全半角统一
4. L4 金额日期格式变体
5. L5 模糊匹配/失败标记

**双坐标证据映射**：建立 normalized_character_index ↔ clean_raw_text_character_index 映射，前端在清洗后原始文本快照中高亮证据，不依赖实时网页 DOM。

### 2.4 来源谱系与版本追踪

- **来源角色判定**：official_original / official_repost / commercial_repost / unknown
- **同源转载识别**：SimHash 汉明距离 ≤ 3，避免将转载数量误判为独立交叉验证
- **事实断言键**（FactAssertionKey）：跨源比较前确保双方表达同一业务事实
- **页面版本追踪**：历史版本不被新版本覆盖

### 2.5 展示等级与选择性输出

- **三级分类**：high / review / low（基于抽取支持度 + 来源质量 + 交叉验证状态）
- **四种输出策略**：strict / default / loose / audit

### 2.6 6 个 MVP 观察信号（v4.1 §9.2，严格不输出信用评分）

| 信号 | 说明 | 归因 |
|---|---|---|
| 中标活跃度 | 近 90 天公开中标次数和金额趋势 | 不作正负定性 |
| 公开中标集中度 | 当前覆盖数据中 Top 3 采购人及地区占比 | 不称"客户集中度" |
| 废标公告关联 | 企业在废标或流标公告中被观察到的次数 | 不直接归因 |
| 明确投标否决 | 公告明确写明企业投标被否决，并记录原因 | 仅记录事实 |
| 信息冲突观察 | 相同事实断言在不同有效来源中出现矛盾 | 仅观察不评判 |
| 高频共现提示（选做） | 企业与其他企业在同一标段被反复观察到 | **不用于判断围标** |

**严谨表述**：使用「公开公告中观察到的投标出现次数」，不得使用「企业实际投标次数」；高频共现必须附带说明「仅凭共现不能判断企业关联关系或围标行为」。

### 2.7 合规采集（v4.1 §5）

| 组件 | 能力 |
|---|---|
| DomainRateLimiter | 域名级频率限制，默认8秒间隔，按域名独立计数，失败回滚 reservation |
| RobotsChecker | robots.txt 合规检查，30分钟域名级缓存，不可达时默认允许 |
| SourceWhitelist | 来源白名单，支持运行时下架/重新启用，集成到 scraper 前置检查 |
| DataDeletionService | 5种范围数据删除（来源URL/平台/公告实例/页面快照/用户授权），记录审计日志 |

失败回退：触发 403/封禁时停止访问，不进行规避；不绕过登录墙、不抓取付费内容。

### 2.8 凭证安全（v4.1 §13.1）

| 凭证类型 | 方案 |
|---|---|
| API Key | 高熵随机值 + HMAC-SHA256 摘要（服务端密钥）+ secrets.compare_digest 防时序攻击 |
| 密码 | Argon2id 哈希（防彩虹表/暴力破解）|
| Cookie | AES-GCM 加密（nonce 唯一）|
| SSRF 防护 | 仅允许 HTTP/HTTPS，拦截内网/回环/链路本地/云元数据地址，重定向后重新检查 |
| 路径安全 | 白名单存储目录，禁止路径穿越，文件名与真实存储键分离 |
| 日志 | 不得记录凭证 |

### 2.9 12 个标准 API 端点（v4.1 §12）

| 接口 | 方法 | 说明 |
|---|---|---|
| /api/projects/search | GET | 搜索采购项目 |
| /api/projects/{project_id} | GET | 获取项目及公告生命周期 |
| /api/notices/{notice_id} | GET | 获取公告详情 |
| /api/notices/{notice_id}/sources | GET | 获取来源页面和谱系 |
| /api/notices/{notice_id}/participants | GET | 获取公告参与方列表 |
| /api/sources/{source_id}/versions | GET | 获取页面版本历史 |
| /api/fields/{field_id} | GET | 获取字段和全部证据 |
| /api/organizations/search | GET | 搜索组织实体 |
| /api/organizations/{org_id} | GET | 获取组织实体公开活动画像 |
| /api/extract/tasks | POST | 提交异步抽取任务 |
| /api/extract/tasks/{task_id} | GET | 查询任务状态 |
| /api/stats/quality | GET | 获取数据质量和评测统计 |

抽取任务异步状态：queued / running / partially_succeeded / succeeded / failed

---

## 三、评测数据（v4.1 W3 真实数据）

### 3.1 数据集

| 项目 | 数值 |
|---|---|
| 数据库公告总数 | 107 篇（成稿时点；最新 162 篇） |
| W3 评测集 | 100 篇（ccgp_w3）|
| 实时采集 | 7 篇（ccgp）|
| 公告类型覆盖 | tender 34 / award 35 / correction 33 / 其他 5 |
| 金标字段总数（99 篇全量）| 594 |
| 官方来源适配器 | 2 个（ccgp + ggzy_national）|

### 3.2 4 组消融实验（99 篇全量，commit caeacde）

> 注：本节为成稿时点快照；598 篇金标全量复测结果见正式提交材料第六节（D 组 field_precision 96.44%、unjustified_rate 0.00%、evidence_precision 100%）。

| 指标 | A组 Direct LLM | B组 LLM+候选证据 | C组 LLM+程序验证 | D组 完整BidAgent |
|---|---|---|---|---|
| unjustified_rate | **100.00%** | 0.00%（失真）| **6.04%** | **0.00%** |
| field_precision | 91.97% | 92.58% | 92.58% | 92.17% |
| evidence_precision | N/A | N/A | 100.00% | 100.00% |

**结论**：
- unjustified_rate A→C 下降约 94 个百分点，证据验证流水线可定位、可抑制无依据输出
- D 组通过选择性输出（display_grade）将 unjustified_rate 进一步降至 0%
- A=B<C（B 组因未验证而失真），证明只有"验证"环节才真正提升精确率
- D 组基于 v4.1 §10.11 Baseline 公平性约束复用 C 组 LLM 调用，仅增加 display_grade

### 3.3 v4.1 §10 新指标 null_false_positive_rate（5 篇重跑）

| 组 | should_not_have_value_fields | null_false_positives | null_false_positive_rate |
|---|---|---|---|
| A/B/C/D | 6 | 0 | **0.0000** |

指标定义：金标为 absent/not_applicable 的字段中，系统错误输出值的比例。两次独立实验均验证为 0.0。

### 3.4 测试

- **1882 passed**（成稿时点；最新 **1979 passed**）· 0 errors / 0 failures
- 27 warnings（预存的 asyncio mark 装饰同步函数告警，与功能无关）
- 测试覆盖：rate_limiter / robots_checker / data_deletion / source_whitelist / repost_features / credentials(45用例,分支100%) / v41_api / v41_fields / demo_pages_smoke / real_demo_api 等

---

## 四、Web Demo（8 页）

| 页面 | 路径 | 核心功能 |
|---|---|---|
| 工作台 | /ui | KPI卡片 + 6步Pipeline + 四大能力卡片 |
| 招标检索 | /ui/search | 跨平台项目搜索 |
| 公告列表 | /ui/notices | 107篇公告列表 |
| 证据验证详情 | /ui/notices/{id} | 字段+证据+双坐标高亮 |
| 组织画像 | /ui/org/{name} | 六维公开活动观察度 + 雷达图 |
| 质量评测 | /ui/quality | KPI + 消融实验 + IoU分布 |
| 版本历史 | /ui/versions | SHA256版本时间线 |
| 智能问答 | /ui/chat | AI辅助问答 |

布局：Ant Design Pro 企业级风格，左侧 Sidebar（8导航项）+ 顶部 Header（3状态徽标）+ 内容区。1366×768 适配。

---

## 五、合规边界

### 5.1 不输出信用评分（v4.1 §9.1）

标小智**不输出供应商信用评分**。所有信号只反映系统覆盖来源和时间范围内观察到的公开招投标活动，供人工尽调参考。公开招投标数据不足以代表企业整体信用。

### 5.2 MVP 暂不实施

- 企业信用评分 / 授信建议 / 中标概率预测 / 围标自动判定
- 全品类 BOQ 异常检测
- 分布式采集 / 图数据库 / 多租户系统
- 商业平台账号池及验证码自动处理
- PDF/OCR 深度解析

> 历史代码中存在的 BOQ 异常检测与废标风险预警模块为早期实验性实现，v4.1 MVP 不包含这些能力，不作为对外功能宣传。

### 5.3 数据来源合规

- 2 个官方来源适配器（ccgp + ggzy_national），均为公开发布信息
- 域名级 8 秒频率限制 + robots.txt 合规检查 + 来源白名单
- 遇 HTTP 403 立即停止抓取，不切换 UA、不更换代理重试
- 不绕过登录墙、不抓取付费内容

---

## 六、技术栈

| 层 | 技术 |
|---|---|
| 后端框架 | Python 3.11+ / FastAPI |
| Agent 框架 | 纯 Python 轻量级实现（不依赖 langgraph）|
| 抓取引擎 | Playwright (async API) + httpx AsyncClient |
| LLM | DeepSeek |
| 任务调度 | APScheduler + croniter |
| 去重算法 | jieba 分词 + 64 位 SimHash |
| 数据库 | SQLite (MVP) → PostgreSQL (生产) |
| ORM | SQLAlchemy 2.0 async + aiosqlite |
| 部署 | Docker 多阶段构建 + non-root 用户 + healthcheck |
| 前端 | 原生 HTML + Ant Design Pro 风格 + ECharts + Phosphor Icons |

---

## 七、工程规范

- 所有中间件用 async/await，不用 callback 风格
- API Key 用 HMAC-SHA256 摘要 + Argon2id 密码哈希 + AES-GCM Cookie 加密
- 环境变量密钥用 `secrets.token_hex(32)` 生成 64 字符 hex
- SSRF 三层防护 / LIKE 注入防护 / 邮件头注入防护 / 路径穿越防护
- 异步函数用 `run_in_executor` 卸载同步 CPU/IO 任务
- 结构化日志带 request_id 上下文，不记录凭证
- 统一错误响应 `{code, data, msg}`
- Git 提交到 feature 分支，禁止直推 main/develop

---

## 八、当前完成状态

### 已完成

| 项目 | 状态 |
|---|---|
| 四层实体数据模型 | ✅ 已实现（ULID 主键）|
| 六 Agent 协同架构 | ✅ 已实现 |
| 可验证抽取引擎 | ✅ 已实现（5级降级 + 双坐标映射）|
| 来源谱系与版本追踪 | ✅ 已实现（SimHash + 事实断言键）|
| 展示等级与选择性输出 | ✅ 已实现（display_grade + 4策略）|
| 6 个 MVP 观察信号 | ✅ 已实现（不输出信用评分）|
| 合规采集 | ✅ 已实现（域名级频率限制 + robots.txt + 白名单 + 数据删除）|
| 凭证安全 | ✅ 已实现（HMAC-SHA256 + Argon2id + AES-GCM，45测试覆盖）|
| 12 个标准 API 端点 | ✅ 已实现 |
| Web Demo 8 页 | ✅ 已实现（Ant Design Pro 风格）|
| 4 组消融实验 | ✅ 已跑通（99篇全量）|
| null_false_positive_rate | ✅ 已验证（=0.0）|
| 1882 单元测试 | ✅ 全通过 |
| GOAI 初赛提交材料 | ✅ v4.1 对齐（作品简介 + PPT + 合规声明）|
| GitHub 仓库 | ✅ 已推送（feature/glm-w4-k3-data, commit 396a3e8）|

### 已知限制

1. **金标数量 107 篇**：未达 v4.1 推荐 300～350 篇，复赛阶段补强
2. **未划分开发集/校准集/测试集**：当前为统一金标集
3. **temperature 记录口径**：记录 0.0，实际 0.1，不影响指标结论
4. **Demo 视频降级处理**：以代码仓库 + Web Demo 8 页作为等价可验证材料
5. **2 个官方来源适配器**：MVP 冻结范围，商业平台暂不接入

---

## 九、GOAI 初赛提交清单

| # | 提交项 | 必需性 | 状态 |
|---|--------|--------|------|
| 1 | 作品简介（500字）| 必需 | ✅ v4.1 对齐 |
| 2 | 方案 PPT（28页）| 必需 | ✅ v4.1 对齐（去除BOQ/信用评分违规页）|
| 3 | 合规边界声明 | 建议附 | ✅ v4.1 对齐 |
| 4 | Demo 视频 | 可选加分 | 降级处理（复赛补录）|
| 5 | 代码仓库链接 | 建议附 | ✅ https://github.com/tlyyxjz/BidAgent |
| 6 | Web Demo 8 页 | 内部交付 | ✅ 已入库 |

---

## 十、请千问 3.8 Max 审查要点

1. **架构合理性**：四层实体模型 + 六 Agent 协同是否清晰？可验证抽取引擎（LLM生成候选 + 确定性程序验证）的设计是否站得住？
2. **功能完整度**：对照 v4.1 规划，MVP 必做项是否齐全？有无遗漏的关键能力？
3. **评测严谨性**：4 组消融实验设计是否合理？null_false_positive_rate=0.0 是否可信？107 篇金标的统计效力是否足够？
4. **合规性**：不输出信用评分 / 不判断围标 / 不实施 BOQ 的边界是否清晰？合规采集（频率限制/robots.txt/白名单/数据删除）是否到位？
5. **凭证安全**：HMAC-SHA256 + Argon2id + AES-GCM 的组合是否符合 v4.1 §13.1 要求？
6. **已知限制**：5 项已知限制是否需要补充？有没有遗漏的风险点？
7. **初赛竞争力**：当前状态在 GOAI AI+金融赛道初赛中的竞争力如何？有哪些可以快速提升的短板？

---

## 附：关键文件路径

| 文件 | 路径 |
|---|---|
| 代码仓库 | <项目根目录>/ |
| v4.1 总规划 | docs/BidAgent_项目总体规划_v4.1_执行定稿版.md |
| GOAI 提交材料 | GOAI_初赛提交材料_正式版.md |
| 合规声明 | _w2_report/compliance.md |
| 提交清单 | _w2_report/GOAI_初赛提交清单.md |
| 方案 PPT | _w2_report/proposal.pptx |
| W3 评测报告 | _w3_outputs/w3_ablation_smoke_v41_rerun_report.md |
| 99 篇全量消融 | _w3_outputs/w3_ablation_full_99.json |
| Bootstrap CI | _w3_outputs/w3_bootstrap_ci_full_99.json |
| 金标冻结 | tests/fixtures/gold/gold_frozen_v1.json |
| GitHub | https://github.com/tlyyxjz/BidAgent |

---

> 团队：标小智（徐浚钊、王祯明）· 上海建桥大学 · GOAI 2026
